# Anthropic API Key (for Claude models)
# Get yours at: https://console.anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# OpenAI API Key (for GPT models)
# Get yours at: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key-here

# xAI API Key (for Grok models)
# Get yours at: https://console.x.ai/
XAI_API_KEY=your-xai-api-key-here

# Browser Automation Configuration
# ================================
# Browser tools via puppeteer-mcp-server are ENABLED BY DEFAULT.
# The puppeteer-mcp-server is auto-downloaded via npx when needed.
# Requires: Node.js/npx installed
#
# If npx is not available, the agent will continue without browser tools.

# Chrome remote debugging port (default: 9222)
# Used by puppeteer_connect_active_tab to connect to existing Chrome instances
# CHROME_DEBUG_PORT=9222

# To disable browser tools, use the --no-browser CLI flag
